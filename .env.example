# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# general role (leaf agents + all dev iteration) — Gemini Flash, free tier.
# Create at https://aistudio.google.com/apikey  (no credit card, no expiry)
GEMINI_API_KEY=

# planning role (orchestrator) — Claude Sonnet, prepaid credit.
# Create at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
